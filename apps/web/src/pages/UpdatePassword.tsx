import {useEffect,useState} from "react";import {consumeRecoverySession,updatePassword} from "../services/authService";export default function UpdatePassword(){const [password,setPassword]=useState(""),[message,setMessage]=useState("");useEffect(()=>{if(!consumeRecoverySession())setMessage("Open this page from a valid password-reset link.")},[]);return <main style={{maxWidth:420,margin:"80px auto"}}><h1>Update password</h1><input type="password" placeholder="New password" value={password} onChange={e=>setPassword(e.target.value)}/><button disabled={password.length<8} onClick={()=>void updatePassword(password).then(()=>setMessage("Password updated."),e=>setMessage(e.message))}>Update password</button><p>{message}</p></main>}
