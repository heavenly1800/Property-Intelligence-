import {useState} from "react";import {Navigate} from "react-router-dom";import {useAuth} from "../context/AuthContext";import {requestPasswordReset} from "../services/authService";
export default function SignIn(){const {session,signIn}=useAuth(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);if(session)return <Navigate to="/" replace/>;const submit=async()=>{setBusy(true);setMessage("");try{await signIn(email,password)}catch(e){setMessage(e instanceof Error?e.message:"Sign-in failed.")}finally{setBusy(false)}};return <main style={{maxWidth:420,margin:"80px auto",padding:24}}><h1>Sign in</h1><p>Access your organization’s property workspace.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button disabled={busy||!email||!password} onClick={()=>void submit()}>{busy?"Signing in…":"Sign in"}</button><button disabled={!email} onClick={()=>void requestPasswordReset(email).then(()=>setMessage("Password reset instructions requested."),e=>setMessage(e.message))}>Reset password</button>{message&&<p>{message}</p>}</main>}
