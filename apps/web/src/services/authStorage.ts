const SESSION_KEY="pi_supabase_session",ORG_KEY="pi_selected_organization";export type StoredSession={access_token:string;refresh_token:string;expires_at?:number;user:{id:string;email?:string}};export const getSession=():StoredSession|null=>{try{return JSON.parse(localStorage.getItem(SESSION_KEY)||"null")}catch{return null}};export const setSession=(value:StoredSession|null)=>value?localStorage.setItem(SESSION_KEY,JSON.stringify(value)):localStorage.removeItem(SESSION_KEY);export const getSelectedOrganization=()=>localStorage.getItem(ORG_KEY);export const setSelectedOrganization=(id:string|null)=>id?localStorage.setItem(ORG_KEY,id):localStorage.removeItem(ORG_KEY);export const clearAuth=()=>{setSession(null);setSelectedOrganization(null)};
